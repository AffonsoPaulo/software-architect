# examples/small-cli-tool/

Placeholder. Will hold a small, realistic example project (e.g. a file-conversion CLI) that skips several phases via Calibration and demonstrates Agile confirmation mode plus a second incremental `cycle`.

Populated in `plan-24-examples-walkthrough.md`.
