# ENT-001 — Tenant
*Kind: Entity · Traces to: UC-001*

| Attribute | Meaning |
|---|---|
| name | The customer organization's display name, shown throughout the UI and in support/billing contexts. |
| created at | When the tenant account was provisioned. |

**Invariants**
- A tenant's identifier, once assigned, never changes and is never reused, even if the tenant is deactivated.

**Belongs to aggregate**: itself is the root
